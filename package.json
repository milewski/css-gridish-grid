{
  "name": "css-gridish-grid",
  "version": "0.0.1",
  "description": "Only the Grid from IBM/css-gridish as an NPM module",
  "main": "./distribution/main/gridish.min.js",
  "module": "./distribution/module/gridish.min.js",
  "files": [
    "distribution/**/gridish*",
    "declaration/**/*.d.ts"
  ],
  "types": "declaration/Gridish.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "prebuild": "rm -rf distribution/ && rm -rf declaration/",
    "prestart": "gridel",
    "build": "LIB=Gridish MODULE_DIR=distribution/modules MAIN_DIR=distribution/main run-s build:* prepare-distribution",
    "build:module": "tsc --module es2015 --target es2015 --outDir $MODULE_DIR && npm run minify -- -o $MODULE_DIR/$LIB.min.js -- $MODULE_DIR/$LIB.js",
    "build:main": "tsc --module commonjs --target es5 --outDir $MAIN_DIR && npm run minify -- -o $MAIN_DIR/$LIB.min.js -- $MAIN_DIR/$LIB.js",
    "prepare-distribution": "for file in distribution/**/*; do sed -i '' \"s/$LIB.js/${LIB,,}.js/g\" $file && mv -v $file `echo ${file,,}`; done;",
    "minify": "uglifyjs --compress --mangle --source-map",
    "start": "babel source/Gridish.ts -x .ts --watch --out-file dev/Gridish.js --source-maps inline | http-server dev -o"
  },
  "author": "Rafael Milewski",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.40",
    "@babel/core": "^7.0.0-beta.40",
    "@babel/plugin-proposal-class-properties": "^7.0.0-beta.40",
    "@babel/plugin-transform-classes": "^7.0.0-beta.40",
    "@babel/plugin-transform-typescript": "^7.0.0-beta.40",
    "@babel/preset-es2015": "^7.0.0-beta.40",
    "@types/node": "^9.4.6",
    "gridel": "0.0.11",
    "http-server": "^0.11.1",
    "npm-run-all": "^4.1.2",
    "typescript": "^2.7.2",
    "uglify-es": "^3.3.9"
  }
}
